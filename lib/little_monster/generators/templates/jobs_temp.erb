class <%= @job_name.camelize %> < LittleMonster::Job
  <% if @job_tasks.size == 0 %>
    task_list :something_to_do
  <% else %>
    task_list :<%= @job_tasks.join(", :") %>
  <% end %>

  def on_cancel
    #Code me!
  end
end
