class <%= @job_name.camelize %> < LittleMonster::Job
  <% if !@task_names || @task_names.size == 0 %>
    task_list :something_to_do
  <% else %>
    task_list :<%= @task_names.join(", :") %>
  <% end %>

  def on_cancel
    #Code me!
  end
end
